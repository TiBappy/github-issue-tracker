1️⃣ What is the difference between var, let, and const?

Ans: Var is an old variable type of javascript .Therefore Let and Const the newest method of ES6 .let and Const has main difference are let take and assign the new value but const don't.

2️⃣ What is the spread operator (...)?

Ans: Spread operator is special syntax in javscript to expand array and string into indivual elements.

3️⃣ What is the difference between map(), filter(), and forEach()?

Ans: (forEach): foreach is mainly a loop in javascript .It looping single element .It doesn't return new array.
    (map): map is mainly transform the array .used of map is creating a new array using single element .It's return a new array .Array size could same .
    (filter): New element collect by using logic is callde filter it return new array .Size could be same or decrease .

4️⃣ What is an arrow function?

Ans: Arrow function is a shorter version of javascript function .It first introduce for ES6 owing to cleaning code & added extra functionality.

5️⃣ What are template literals?

Ans: Template literals is the modern feature of ES6 .Which using the string manipulate easily and embed the variable .It's look like (``);